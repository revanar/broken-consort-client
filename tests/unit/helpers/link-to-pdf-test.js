import { linkToPdf } from 'client/helpers/link-to-pdf';
import { module, test } from 'qunit';

module('Unit | Helper | link to pdf');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = linkToPdf([42]);
  assert.ok(result);
});
